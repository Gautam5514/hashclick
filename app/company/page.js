import AboutPage from "../components/about/AboutPage";

export const metadata = {
  title: "About Us & Company | Hashboard",
  description: "We build software that gives people back the time their tools were quietly taking. Discover Hashboard's mission, team, and values.",
};

export default function Page() {
  return <AboutPage />;
}
