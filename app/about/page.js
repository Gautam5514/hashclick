import AboutPage from "../components/about/AboutPage";

export const metadata = {
  title: "About Us | Hashboard",
  description: "We believe project management is broken. There has to be a better way to work; a more efficient way to be productive; a happier way to get shit done. That's why we created Hashboard.",
};

export default function Page() {
  return <AboutPage />;
}
