import ContactPage from "../components/contact/ContactPage";

export const metadata = {
  title: "Contact Us | Hashboard",
  description: "Get in touch with the Hashboard team — sales, support, partnerships, or press.",
};

export default function Page() {
  return <ContactPage />;
}
