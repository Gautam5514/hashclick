import ProjectsPage from "../../components/projects/ProjectsPage";
import "../../components/tasks/tasks.css";

export const metadata = {
  title: "Hashboard Projects — Plan work and keep teams moving",
  description: "Bring project goals, tasks, owners, deadlines, conversations, and progress together in one connected workspace.",
};

export default function Page() {
  return <ProjectsPage />;
}
