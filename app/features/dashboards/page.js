import DashboardsPage from "../../components/dashboards/DashboardsPage";
import "../../components/tasks/tasks.css";

export const metadata = {
  title: "Dashboards: Build custom reports you can actually work from | Hashboard",
  description: "Build live, actionable dashboards in Hashboard so teams can see performance, update work, and act from the same place.",
};

export default function Page() {
  return <DashboardsPage />;
}
