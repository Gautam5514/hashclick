import DashboardsPage from "../../components/dashboards/DashboardsPage";
import "../../components/tasks/tasks.css";

export const metadata = {
  title: "Dashboards: Build custom reports you can actually work from | ClickUp",
  description: "Build live, actionable dashboards in ClickUp so teams can see performance, update work, and act from the same place.",
};

export default function Page() {
  return <DashboardsPage />;
}
